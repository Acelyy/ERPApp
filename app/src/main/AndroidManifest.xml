<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.huangxinhui.erpapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="designwidth"
            android:value="1080" />
        <meta-data
            android:name="designdpi"
            android:value="480" />
        <meta-data
            android:name="fontsize"
            android:value="1.0" />
        <meta-data
            android:name="unit"
            android:value="dp" />

        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".QueryActivity" />
        <activity android:name=".QueryInformationActivity" />
        <activity android:name=".ReceiveActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReceiveInformationActivity" />
        <activity android:name=".OutActivity" />
        <activity android:name=".IntoActivity" />
        <activity android:name=".DeleteActivity" />
        <activity android:name=".IntoInformationActivity" />
        <activity android:name=".OutInformationActivity" />
        <activity android:name=".DeleteInformationActivity"></activity>
    </application>

</manifest>